name: flutter_card_scanner_latest
description: A flutter project for scanning the credit cards on an iOS device.
version: 1.0.3
homepage: https://flutter.io/
repository: https://github.com/SuryaPrabha27/flutter-card-scanner

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
